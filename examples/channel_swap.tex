// Channel Swap — swap red and blue channels
//
// Simple RGB-to-BGR channel reorder. Useful for correcting images
// with swapped channel order.
//
// Connect @image to any RGB input.

@OUT = vec3(@image.b, @image.g, @image.r);
