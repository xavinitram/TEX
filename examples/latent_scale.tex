// Latent scale — adjust latent intensity
// Connect latent data to @latent
// Output type is auto-inferred as LATENT

f$scale = 1.1;   // scale factor (e.g. 1.1 for slight boost)

@OUT = @latent * $scale;
