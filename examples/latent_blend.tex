// Latent blend — mix two latent tensors
// Connect latent data to @latent_a and @latent_b
// Output type is auto-inferred as LATENT

f$blend = 0.5;   // 0.0 = all A, 1.0 = all B

@OUT = lerp(@latent_a, @latent_b, $blend);
