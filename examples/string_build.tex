// Build a sanitized filename from parts
// Uses string parameters with defaults

s$prefix = "image";
s$suffix = ".png";

string name = $prefix + "_processed_" + $suffix;
@OUT = sanitize_filename(name);
